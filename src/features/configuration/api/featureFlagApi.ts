import type { AxiosResponse } from 'axios'

export async function getFeatureFlags(): Promise<AxiosResponse | null> {
  return Promise.resolve(null)
}

export async function updateFeatureFlags(): Promise<AxiosResponse | null> {
  return Promise.resolve(null)
}

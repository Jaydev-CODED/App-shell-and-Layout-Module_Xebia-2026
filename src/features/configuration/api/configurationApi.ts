import type { AxiosResponse } from 'axios'

export async function getConfiguration(): Promise<AxiosResponse | null> {
  return Promise.resolve(null)
}

export async function updateConfiguration(): Promise<AxiosResponse | null> {
  return Promise.resolve(null)
}
